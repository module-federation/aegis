{
  "adapters": {
    "desc": "Example .env file",
    "datasources": {
      "DataSourceMemory": {
        "desc": "Non-persistent, in-memory storage",
        "enabled": false
      },
      "DataSourceFile": {
        "desc": "Persistent storage on local file system",
        "enabled": false
      },
      "DataSourceMongoDb": {
        "desc": "Persistent storage in NoSQL, JSON document db",
        "enabled": false
      },
      "DataSourceMeshLink": {
        "desc": "Data distributed across service mesh network",
        "enabled": true
      }
    }
  },
  "services": {
    "serviceMesh": {
      "WebSwitch": {
        "desc": "Default implementation: switched mesh over web sockets.",
        "enabled": false
      },
      "MeshLink": {
        "desc": "Fast, UDP-based, peer-to-peer mesh, with shared Redis cache.",
        "enabled": true,
        "config": {
          "redis": {
            "host": "127.0.0.1",
            "port": 6379
          },
          "prefix": "aegis",
          "strict": false,
          "relayLimit": 1,
          "relayDelay": 0,
          "updateInterval": 1000
        }
      },
      "QuicMesh": {
        "desc": "Mesh over QUIC transport protocol. Replaces TCP in HTTP/3. Optimized for streaming.",
        "enabled": false
      },
      "NatsMesh": {
        "desc": "Mesh uses NATs at Layer 7.",
        "enabled": false
      }
    }
  }
}
