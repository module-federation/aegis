/**
 * Broadcast to aegis host instances using multicast groups.
 * Each domain model / app gets its own multicast group / address.
 *
 * The idea is to form a network overlay that divides the network
 * into application domains, and routes messages accordingly.
 */
